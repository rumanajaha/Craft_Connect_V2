import LoginForm from "@/components/auth/login-form";

export const metadata = {
  title: "Log In — CraftConnect",
  description: "Log in to your CraftConnect account to access your dashboard.",
};

export default function LoginPage() {
  return <LoginForm />;
}
