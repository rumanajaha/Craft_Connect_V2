import ResetForm from "@/components/auth/reset-form";
export const metadata = {
    title: "Reset Password — CraftConnect",
    description: "Reset your CraftConnect password.",
};
export default function ResetPasswordPage() {
    return <ResetForm />;
}
